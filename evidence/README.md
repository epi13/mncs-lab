# Evidence

Store reproducible experimental evidence here when results are too substantial for an RFC or pressure entry alone.

Useful evidence includes latency distributions, memory growth, invalidation sets, JIT generation maps, diagnostic recovery cases, backend comparisons, and proof-check timing.

Prefer small machine-readable summaries plus enough metadata to reproduce the run. Avoid committing enormous raw traces when a compact derived artifact is sufficient.
