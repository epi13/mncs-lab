# Source

Implementation will live here once an executable experiment has a clearly defined boundary.

Policy:

- prefer MNCS-language for lab-owned logic;
- consume compiler-owned parsing/type/proof/IR/lowering behavior;
- do not build a shadow compiler here;
- label unavoidable host bootstrap code and connect it to a pressure entry when it substitutes for a missing capability.
