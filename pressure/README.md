# Language and Compiler Pressure Evidence

This directory records concrete pressure discovered while implementing and experimenting with `mncs-lab`.

Read `docs/language-pressure.md` and RFC 0006.

Do not pre-populate speculative gaps as confirmed bugs. Expected pressure domains belong in RFCs and experiment plans; registry entries require an attempted workload and reproducible evidence.

Minimal reproducers belong under `pressure/reproducers/` when practical.
